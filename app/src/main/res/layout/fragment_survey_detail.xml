<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.mahdiye.csns.SurveyDetailActivityFragment"
    tools:showIn="@layout/activity_survey_detail">

    <TextView
        android:id="@+id/survey_detail_textview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Button
        android:layout_width="@dimen/button_survey_submit_width"
        android:layout_height="@dimen/button_survey_submit_height"
        android:text="@string/button_survey_submit"
        android:id="@+id/button_survey_submit"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@color/colorPrimary"
        android:textColor="@color/colorTextView"
        android:textStyle="bold"/>


</RelativeLayout>
